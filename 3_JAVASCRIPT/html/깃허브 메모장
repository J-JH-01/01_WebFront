git : 코드 추적 통합 관리 프로그램 - 버전관리 시스템
github : gitlab'

1) ls(list) 치기 :현재 디렉토리(폴더)의 파일 목록 확인

2) cd(change directory) : 현재 작업중인 디렉토리 변경 , 이동에 관한것

  cd directoryname : 해당 디렉토리로 이동 
  ex) cd 3_Javascript/ (3_ 까지만 치고 탭 누르면 자동완성)

  cd .. : 현재 디렉토리의 부모 디렉토리로 이동

  git init : 새로운 Git 저장소를 초기화할 때 사용,
    -현재 디렉토리를 Git 저장소로 사용
    -이후에는 해당 디렉토리에서 Git의 버전관리 기능을 이용할 수 있음
    -프로젝트를(여러분폴더) Git으로 관리하려고 할 때 가장 먼저 사용하는 명령어
    치면 마스터가 뜨는데 이 때부터는 깃이 관리하는 폴더라는 것임
    
  웹프론트파일에서 - 보기 - 숨긴파일 보기 체크하면 .git이 뜨는데
  이건 해당 디렉토리를 git이 관리한다는 것임. 그래서 지우면 안된다
  현재는 로컬(내 pc)에서 일어나는 변경사항만 추적/관리

git status :현재 스테이징 영역에 있는 파일과 아닌 파일들의 상태를 확인
    무대로 못간 파일들이 빨간 글씨로 뜬다, 저장할 준비조차 못됬다는 것
  ex)git add . :치고 status 다시치면 초록글씨로 무대로 올라간 아이들이 보임 이 상태에서 초록이들 저장 가능

  gid add : 파일을 스테이징 영역에 올리는 것

   ->git add 파일명 
    ex ) git add test.html : test.html 파일을 스테이징 영역에 올림
   ->git add.: 프로젝트 내 모든 파일을 스테이징 영역에 올림( . : 모든파일 의미)  stage 무대 (초록글씨영역)

  -스테이지무대에 온 얘들을 하나의 버전으로 만들어 줄 것임
  
  git commit - 스테이징 영역에 잇는 변경사항을 커밋하여 로컬 저장소에 기록하는데 사용(하나의 버전으로 만든다)

  git 사용자 설정 & 환경 설정
  git commit -m ( -붙은건 메세지) "first commit"
  git commit -m "240826 js 함수파트 끝" << 이런식으로 작성하면 된다
    (이 단계에서 깃허브 연결 메세지같은거 뜨는데 그때 이메일,이름 쳐서 로그인하면 된다)
  git config --global user.name 치면 내 이름 나옴, 로그인 단에서는 "이름"을 뒤에 덧붙혀서 치면된
  git config --global user.email 치면 내 이메일 나옴, 로그인 단에서는 "이메일"을 뒤에 덧붙혀서 치면됨

  git remote add origin https://github.com/J-JH-01/01_WebFront.git 깃허브 주소 연결
  
  bush 창에서 shift + insert 키 : 복사

  git push : 로컬 페파지토리에 잇는 commit 내역을 원격 레파지토리로 올리는 것
    -> 업스트리밍 쓰라고 나오는데 git push --set-upstream origin master
     복사 ctrl +c로 하고 복붙






  1.git pull : 원격에서 로컬로 당기시오(내려받으시오) 항상 집/ 학원 컴퓨터 키면 바로 git pull
    ~ 코드 치고 ... 공부하고... 
  2.git commit :
  3.git push : 로컬에서 원격(깃 서버)으로 밀어넣으시오 (올리는거)


  학원 pc <----> 원격 깃 <----> 개인 pc 

  : 이런식으로 연결되어 있는 것임

  git clone 원격저장소주소(사이트에서 복붙가능)
  :다른 로컬 pc에 원격저장소에 있는 프로젝트 클론 만들기

  -------------------------------------------------------
  ls 
  
  cd 01_WebFront/

  code .  << 이거 치면 뜸 학원이든, 집이든 어디서든 이렇게 불러오면 된다
  ----------------------------------------

 변경사항있으면 해당 폴더 똑같이 찾아가서

 git add . 하고 status로 확인한번해보고

 git commit -m "버전명 메세지"

 git push 로 올려주기


 확실히 해둘것 !!!!!!!!!

 다운로드는 git pull 하나면 되지만 올리는건 여러단계 해야함!!!!!!!!!!!

 이렇게 쫌 많이 연습해두면 좋음
  ---------------------------------

  사이트 : 저장소에서 만들기 누르기

  이름,public만 하면 된다

